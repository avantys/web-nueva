---
---

<section class="faq-section">
    <div class="container">
        
        <!-- Header -->
        <div class="section-header">
            <h2 class="section-title">Preguntas Frecuentes sobre VPS</h2>
            <p class="section-subtitle">Resolvemos todas tus dudas sobre servidores virtuales privados, administración y servicios incluidos</p>
        </div>

        <!-- Search Box -->
        <div class="faq-search">
            <input type="text" class="faq-search-input" placeholder="🔍 Buscar en preguntas frecuentes..." id="faqSearch">
        </div>

        <!-- Categories -->
        <div class="faq-categories">
            <div class="faq-category active" data-category="all">Todas</div>
            <div class="faq-category" data-category="general">General</div>
            <div class="faq-category" data-category="tecnico">Técnico</div>
            <div class="faq-category" data-category="precio">Precios</div>
            <div class="faq-category" data-category="soporte">Soporte</div>
        </div>

        <!-- FAQ Container -->
        <div class="faq-container">
            
            <!-- FAQ 1 -->
            <div class="faq-item" data-category="general">
                <div class="faq-question">
                    <h3>¿Qué es un VPS? Servidor Virtual Privado [Guía 2024]</h3>
                    <div class="faq-icon"></div>
                </div>
                <div class="faq-answer">
                    <div class="faq-answer-content">
                        Un servidor VPS (Virtual Private Server) es un entorno de hosting con <strong>recursos dedicados y garantizados</strong> (CPU, RAM y almacenamiento) dentro de un servidor físico. Es ideal para webs de alto tráfico, e-commerce, aplicaciones empresariales y entornos de desarrollo. Nuestros VPS se alojan en <strong>centros Tier III certificados</strong> en España y Europa, con baja latencia y cumplimiento RGPD.
                    </div>
                </div>
            </div>

            <!-- FAQ 2 -->
            <div class="faq-item" data-category="precio">
                <div class="faq-question">
                    <h3>¿Qué precio tienen los VPS administrados de Avantys?</h3>
                    <div class="faq-icon"></div>
                </div>
                <div class="faq-answer">
                    <div class="faq-answer-content">
                        Desde <strong>10,90€/mes (IVA incluido)</strong>. Todo incluido sin extras: administración completa, soporte 24/7, backups diarios, tráfico ilimitado y migración gratuita. Precio final transparente sin sorpresas.
                    </div>
                </div>
            </div>

            <!-- FAQ 3 -->
            <div class="faq-item" data-category="tecnico">
                <div class="faq-question">
                    <h3>¿Qué sistemas operativos puedo elegir?</h3>
                    <div class="faq-icon"></div>
                </div>
                <div class="faq-answer">
                    <div class="faq-answer-content">
                        <strong>Linux:</strong> Ubuntu 22.04 LTS, Debian 12, AlmaLinux 9, Rocky Linux 9. <strong>Windows:</strong> Server 2022, 2025 con licencias Microsoft originales. Todos preconfigurados y optimizados para máximo rendimiento.
                    </div>
                </div>
            </div>

            <!-- FAQ 4 -->
            <div class="faq-item" data-category="soporte">
                <div class="faq-question">
                    <h3>¿Ofrecéis soporte técnico en español?</h3>
                    <div class="faq-icon"></div>
                </div>
                <div class="faq-answer">
                    <div class="faq-answer-content">
                        Sí, soporte especializado en español <strong>24/7 por ticket, chat y teléfono</strong>, con ingenieros certificados. Tiempo de respuesta medio <strong>inferior a 2 horas</strong> en horario laboral y resolución proactiva de incidencias.
                    </div>
                </div>
            </div>

            <!-- FAQ 5 -->
            <div class="faq-item" data-category="general">
                <div class="faq-question">
                    <h3>¿Necesito conocimientos técnicos para usar un VPS?</h3>
                    <div class="faq-icon"></div>
                </div>
                <div class="faq-answer">
                    <div class="faq-answer-content">
                        No con nuestro servicio administrado. Nos encargamos de la instalación, configuración, seguridad y mantenimiento. Tú solo te enfocas en tu proyecto mientras nosotros gestionamos la <strong>infraestructura técnica completa</strong>.
                    </div>
                </div>
            </div>

            <!-- FAQ 6 -->
            <div class="faq-item" data-category="soporte">
                <div class="faq-question">
                    <h3>¿Qué garantía ofrecéis si no estoy satisfecho?</h3>
                    <div class="faq-icon"></div>
                </div>
                <div class="faq-answer">
                    <div class="faq-answer-content">
                        <strong>30 días de garantía total</strong> de devolución para probar el servicio sin riesgo. Si no cumplimos tus expectativas, reembolso completo sin preguntas ni penalizaciones.
                    </div>
                </div>
            </div>

        </div>

        <!-- CTA Section -->
        <div class="faq-cta">
            <h3>¿No encuentras la respuesta que buscas?</h3>
            <p>Nuestro equipo de especialistas en VPS está aquí para ayudarte con cualquier duda específica sobre tu proyecto</p>
            <a href="#contacto" class="faq-cta-button">Contactar Especialista</a>
        </div>

    </div>
</section>

<style>
.faq-section {
    padding: 60px 0;
    background: linear-gradient(135deg, var(--color-gray-light) 0%, white 100%);
}

.faq-container {
    max-width: 900px;
    margin: 0 auto;
}

.faq-item {
    background: white;
    border: 1px solid rgba(60, 60, 60, 0.1);
    border-radius: 12px;
    margin-bottom: 16px;
    box-shadow: 0 2px 8px rgba(0, 32, 64, 0.05);
    overflow: hidden;
    transition: all 0.3s ease;
}

.faq-item:hover {
    border-color: var(--color-accent);
    box-shadow: 0 4px 20px rgba(133, 96, 204, 0.1);
}

.faq-item.active {
    border-color: var(--color-accent);
    box-shadow: 0 4px 20px rgba(133, 96, 204, 0.15);
}

.faq-question {
    padding: 24px;
    cursor: pointer;
    display: flex;
    justify-content: space-between;
    align-items: center;
    position: relative;
    transition: all 0.3s ease;
}

.faq-question:hover {
    background: rgba(133, 96, 204, 0.05);
}

.faq-question h3 {
    font-size: 1.2rem;
    font-weight: 700;
    color: var(--color-primary);
    margin: 0;
    padding-right: 40px;
    line-height: 1.4;
    font-family: var(--font-family);
}

.faq-icon {
    position: absolute;
    right: 24px;
    top: 50%;
    transform: translateY(-50%);
    width: 24px;
    height: 24px;
    border: 2px solid var(--color-accent);
    border-radius: 50%;
    display: flex;
    align-items: center;
    justify-content: center;
    transition: all 0.3s ease;
    flex-shrink: 0;
}

.faq-icon::before {
    content: "+";
    font-size: 1.2rem;
    font-weight: bold;
    color: var(--color-accent);
    transition: all 0.3s ease;
}

.faq-item.active .faq-icon {
    background: var(--color-accent);
    transform: translateY(-50%) rotate(45deg);
}

.faq-item.active .faq-icon::before {
    color: white;
}

.faq-answer {
    max-height: 0;
    overflow: hidden;
    transition: max-height 0.3s ease;
}

.faq-answer-content {
    padding: 0 24px 24px 24px;
    color: var(--color-secondary);
    line-height: 1.6;
    font-size: 1rem;
    font-family: var(--font-family);
}

.faq-item.active .faq-answer {
    max-height: 500px;
}

.faq-search {
    margin-bottom: 40px;
    text-align: center;
}

.faq-search-input {
    width: 100%;
    max-width: 500px;
    padding: 16px 24px;
    border: 2px solid rgba(60, 60, 60, 0.15);
    border-radius: 50px;
    font-size: 1rem;
    font-family: var(--font-family);
    transition: all 0.3s ease;
    background: white;
}

.faq-search-input:focus {
    outline: none;
    border-color: var(--color-accent);
    box-shadow: 0 0 0 3px rgba(133, 96, 204, 0.1);
}

.faq-search-input::placeholder {
    color: #94a3b8;
}

.faq-categories {
    display: flex;
    justify-content: center;
    gap: 16px;
    margin-bottom: 40px;
    flex-wrap: wrap;
}

.faq-category {
    padding: 8px 20px;
    background: white;
    border: 2px solid rgba(60, 60, 60, 0.15);
    border-radius: 25px;
    cursor: pointer;
    transition: all 0.3s ease;
    font-size: 0.9rem;
    font-weight: 600;
    color: var(--color-secondary);
    font-family: var(--font-family);
}

.faq-category:hover,
.faq-category.active {
    background: var(--color-accent);
    color: white;
    border-color: var(--color-accent);
}

.faq-cta {
    text-align: center;
    margin-top: 60px;
    padding: 40px;
    background: white;
    border-radius: 16px;
    border: 2px solid rgba(133, 96, 204, 0.1);
}

.faq-cta h3 {
    font-size: 1.5rem;
    color: var(--color-primary);
    margin-bottom: 16px;
    font-family: var(--font-family);
}

.faq-cta p {
    color: var(--color-secondary);
    margin-bottom: 24px;
    font-size: 1rem;
    font-family: var(--font-family);
}

.faq-cta-button {
    display: inline-block;
    padding: 12px 32px;
    background: var(--color-accent);
    color: white;
    text-decoration: none;
    border-radius: 8px;
    font-weight: 600;
    transition: all 0.3s ease;
    font-family: var(--font-family);
}

.faq-cta-button:hover {
    background: var(--color-primary);
    color: white;
    transform: translateY(-2px);
    text-decoration: none;
}

@media (max-width: 768px) {
    .faq-question {
        padding: 20px;
    }
    
    .faq-question h3 {
        font-size: 1.1rem;
        padding-right: 50px;
    }
    
    .faq-answer-content {
        padding: 0 20px 20px 20px;
    }
    
    .faq-search-input {
        font-size: 16px;
    }
    
    .faq-categories {
        gap: 8px;
    }
    
    .faq-category {
        padding: 6px 16px;
        font-size: 0.85rem;
    }
    
    .faq-section {
        padding: 40px 0;
    }
}

@media (max-width: 480px) {
    .faq-cta {
        padding: 30px 20px;
    }
}
</style>

<script>
// FAQ Functionality
document.addEventListener('DOMContentLoaded', function() {
    // FAQ Toggle
    const faqItems = document.querySelectorAll('.faq-item');
    const faqQuestions = document.querySelectorAll('.faq-question');
    
    faqQuestions.forEach(question => {
        question.addEventListener('click', function() {
            const faqItem = this.parentElement;
            const isActive = faqItem.classList.contains('active');
            
            // Close all other FAQs
            faqItems.forEach(item => {
                item.classList.remove('active');
            });
            
            // Toggle current FAQ
            if (!isActive) {
                faqItem.classList.add('active');
            }
        });
    });

    // Search functionality
    const searchInput = document.getElementById('faqSearch');
    if (searchInput) {
        searchInput.addEventListener('input', function() {
            const searchTerm = this.value.toLowerCase();
            
            faqItems.forEach(item => {
                const question = item.querySelector('h3').textContent.toLowerCase();
                const answer = item.querySelector('.faq-answer-content').textContent.toLowerCase();
                
                if (question.includes(searchTerm) || answer.includes(searchTerm)) {
                    item.style.display = 'block';
                } else {
                    item.style.display = searchTerm === '' ? 'block' : 'none';
                }
            });
        });
    }

    // Category filtering
    const categories = document.querySelectorAll('.faq-category');
    
    categories.forEach(category => {
        category.addEventListener('click', function() {
            const selectedCategory = this.dataset.category;
            
            // Update active category
            categories.forEach(cat => cat.classList.remove('active'));
            this.classList.add('active');
            
            // Filter FAQ items
            faqItems.forEach(item => {
                if (selectedCategory === 'all' || item.dataset.category === selectedCategory) {
                    item.style.display = 'block';
                } else {
                    item.style.display = 'none';
                }
            });
            
            // Clear search
            if (searchInput) {
                searchInput.value = '';
            }
        });
    });
});
</script>
