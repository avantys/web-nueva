---
---

<section class="network-section">
    <div class="container">
        <div class="section-header">
            <h2 class="section-title">Red e IP Optimizadas</h2>
            <p class="section-subtitle">Conectividad premium sin límites para garantizar máximo rendimiento y accesibilidad global de tu VPS</p>
        </div>

        <div class="content-grid">
            <!-- Tráfico ilimitado -->
            <div class="info-card">
                <i class="fas fa-exchange-alt card-icon"></i>
                <h3 class="card-title">Tráfico ilimitado sin restricciones</h3>
                <p class="card-description">
                    Todos nuestros VPS incluyen <strong>tráfico entrante y saliente completamente ilimitado</strong>, sin costes ocultos, <strong>fair-use policy</strong> ni límites inesperados. Ideal para proyectos de alto volumen.
                </p>
                <div class="status-badges">
                    <span class="badge badge-included">✅ Incluido</span>
                </div>
            </div>

            <!-- Ancho de banda garantizado -->
            <div class="info-card">
                <i class="fas fa-tachometer-alt card-icon"></i>
                <h3 class="card-title">Ancho de banda garantizado premium</h3>
                <p class="card-description">
                    Según el plan VPS seleccionado, dispones de hasta <strong>3 Gbps de ancho de banda público</strong> para soportar <strong>grandes volúmenes de datos</strong> y picos de tráfico sin degradación.
                </p>
                <div class="status-badges">
                    <span class="badge badge-included">✅ Incluido</span>
                </div>
            </div>

            <!-- Dirección IP dedicada -->
            <div class="info-card">
                <i class="fas fa-map-marker-alt card-icon"></i>
                <h3 class="card-title">Dirección IP dedicada y adicionales</h3>
                <p class="card-description">
                    Cada VPS incluye una <strong>IP IPv4 dedicada</strong> desde el primer día, con posibilidad de añadir <strong>IPs adicionales</strong> según tus necesidades de proyectos múltiples o configuraciones avanzadas.
                </p>
                <div class="status-badges">
                    <span class="badge badge-included">✅ Incluido</span>
                    <span class="badge badge-optional">🔵 IPs Extra</span>
                </div>
            </div>

            <!-- Reverse DNS -->
            <div class="info-card">
                <i class="fas fa-network-wired card-icon"></i>
                <h3 class="card-title">Reverse DNS y configuración avanzada</h3>
                <p class="card-description">
                    Gestiona <strong>registros PTR (reverse DNS)</strong>, configuración de hostname y <strong>DNS avanzado</strong> de tu servidor VPS fácilmente desde tu área de cliente sin conocimientos técnicos.
                </p>
                <div class="status-badges">
                    <span class="badge badge-included">✅ Incluido</span>
                </div>
            </div>
        </div>
    </div>
</section>

<style>
.network-section {
    padding: 60px 0;
}

.content-grid {
    display: grid;
    grid-template-columns: repeat(2, 1fr);
    gap: 30px;
    max-width: 1000px;
    margin: 0 auto;
}

.info-card {
    background: white;
    border: 1px solid rgba(60, 60, 60, 0.15);
    border-radius: 16px;
    padding: 32px;
    transition: all 0.3s ease;
    box-shadow: 0 2px 8px rgba(0, 32, 64, 0.08);
    text-align: center;
}

.info-card:hover {
    transform: translateY(-4px);
    box-shadow: 0 8px 25px rgba(133, 96, 204, 0.15);
    border-color: var(--color-accent);
}

.card-icon {
    font-size: 2.8rem;
    color: var(--color-accent);
    margin-bottom: 20px;
    display: block;
    transition: all 0.3s ease;
}

.info-card:hover .card-icon {
    transform: scale(1.1);
    color: var(--color-primary);
}

.card-title {
    font-size: 1.4rem;
    font-weight: 700;
    margin-bottom: 16px;
    color: var(--color-primary);
    line-height: 1.3;
    font-family: var(--font-family);
}

.card-description {
    font-size: 1rem;
    line-height: 1.6;
    color: var(--color-secondary);
    margin-bottom: 24px;
    font-family: var(--font-family);
}

.status-badges {
    display: flex;
    gap: 12px;
    flex-wrap: wrap;
    justify-content: center;
}

.info-card:hover .badge-included {
    background: var(--color-accent);
}

.info-card:hover .badge-optional {
    background: var(--color-primary);
}

@media (max-width: 1024px) {
    .content-grid {
        grid-template-columns: 1fr;
        gap: 24px;
    }
}

@media (max-width: 768px) {
    .info-card {
        padding: 24px;
    }
}
</style>
