---
// src/components/VPSDatacenters.astro
---

<section class="datacenter-section">
    <div class="container">
        <div class="section-header">
            <h2 class="section-title">Centros de Datos Globales</h2>
            <p class="section-subtitle">Infraestructura mundial de última generación para garantizar el mejor rendimiento y disponibilidad de tu VPS</p>
        </div>

        <div class="content-grid">
            <!-- Datacenters worldwide -->
            <div class="info-card">
                <i class="fas fa-globe-europe card-icon"></i>
                <h3 class="card-title">Datacenters en 8+ países</h3>
                <p class="card-description">
                    VPS alojados en centros de datos Tier III certificados en España, Francia, Alemania, Reino Unido, Países Bajos, Canadá, EEUU y Singapur. Conectividad premium internacional con latencia ultra-baja.
                </p>
                <span class="badge badge-included">✅ Incluido</span>
            </div>

            <!-- GDPR Compliance -->
            <div class="info-card">
                <i class="fas fa-shield-alt card-icon"></i>
                <h3 class="card-title">Cumplimiento RGPD y normativas</h3>
                <p class="card-description">
                    Infraestructura 100% alineada con el Reglamento General de Protección de Datos europeo, SOC 2 Type II y normativas internacionales, garantizando máxima privacidad y cumplimiento legal.
                </p>
                <span class="badge badge-included">✅ Incluido</span>
            </div>

            <!-- Redundancy -->
            <div class="info-card">
                <i class="fas fa-sync-alt card-icon"></i>
                <h3 class="card-title">Redundancia total y alta disponibilidad</h3>
                <p class="card-description">
                    Redundancia N+1 en alimentación eléctrica UPS, climatización HVAC, conectividad multi-carrier y sistemas RAID. Generadores diésel de respaldo para continuidad garantizada sin interrupciones.
                </p>
                <span class="badge badge-included">✅ Incluido</span>
            </div>

            <!-- 24/7 Monitoring -->
            <div class="info-card">
                <i class="fas fa-chart-line card-icon"></i>
                <h3 class="card-title">Monitorización proactiva 24/7/365</h3>
                <p class="card-description">
                    Supervisión continua por NOC (Network Operations Center) con ingenieros certificados CISSP, detección automática de incidencias y resolución proactiva con alertas en tiempo real.
                </p>
                <span class="badge badge-included">✅ Incluido</span>
            </div>
        </div>
    </div>
</section>

<style>
.datacenter-section {
    padding: 60px 0;
}

.content-grid {
    display: grid;
    grid-template-columns: repeat(2, 1fr);
    gap: 30px;
    max-width: 1000px;
    margin: 0 auto;
}

.info-card {
    background: white;
    border: 1px solid rgba(60, 60, 60, 0.15);
    border-radius: 16px;
    padding: 32px;
    transition: all 0.3s ease;
    box-shadow: 0 2px 8px rgba(0, 32, 64, 0.08);
    text-align: center;
}

.info-card:hover {
    transform: translateY(-4px);
    box-shadow: 0 8px 25px rgba(133, 96, 204, 0.15);
    border-color: var(--color-accent);
}

.card-icon {
    font-size: 2.8rem;
    color: var(--color-accent);
    margin-bottom: 20px;
    display: block;
    transition: all 0.3s ease;
}

.info-card:hover .card-icon {
    transform: scale(1.1);
    color: var(--color-primary);
}

.card-title {
    font-size: 1.4rem;
    font-weight: 700;
    margin-bottom: 16px;
    color: var(--color-primary);
    line-height: 1.3;
    font-family: var(--font-family);
}

.card-description {
    font-size: 1rem;
    line-height: 1.6;
    color: var(--color-secondary);
    margin-bottom: 24px;
    font-family: var(--font-family);
}

.info-card:hover .badge-included {
    background: var(--color-accent);
}

@media (max-width: 1024px) {
    .content-grid {
        grid-template-columns: 1fr;
        gap: 24px;
    }
}

@media (max-width: 768px) {
    .info-card {
        padding: 24px;
    }
}
</style>
